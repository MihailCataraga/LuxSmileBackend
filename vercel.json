{
    "functions": {
        "api": {
            "runtime": "node@14",
            "handler": "server.js"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/$1"
        }
    ]
}